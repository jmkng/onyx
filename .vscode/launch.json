{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "build --path test",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "args": [
        "build",
        "--path",
        "test"
      ]
    },
    {
      "name": "build --path test --verbose",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}",
      "args": [
        "build",
        "--path",
        "test",
        "--verbose"
      ]
    }
  ]
}